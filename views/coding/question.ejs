<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"/>
  <style>
    body { background-color: #f8f9fa; overflow: hidden; }
    .navbar { background-color: #0d6efd; }
    .navbar-brand, .nav-link { color: #fff !important; }
    .editor-area {
      height: 70vh;
      font-family: monospace;
      background: #fff;
      border: 1px solid #ddd;
      border-radius: 5px;
      padding: 10px;
      resize: none;
    }
  </style>
</head>
<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg shadow-sm">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">Path2Tech</a>
      <div class="collapse navbar-collapse">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item"><a class="nav-link" href="/">Home</a></li>
          <li class="nav-item"><a class="nav-link" href="/modules">MCQs</a></li>
          <li class="nav-item"><a class="nav-link active" href="/coding">Coding</a></li>
        </ul>
      </div>
    </div>
  </nav>

  <div class="container-fluid mt-3">
    <div class="row">
      <!-- Left Half -->
      <div class="col-md-6 border-end p-4" style="height: 85vh; overflow-y: auto;">
        <h3 class="fw-bold text-primary mb-3"><%= question.title %></h3>
        <p><strong>Subject:</strong> <%= question.subject %></p>
        <p>
          <strong>Difficulty:</strong>
          <span class="badge 
            <% if (question.difficulty === 'Easy') { %> bg-success 
            <% } else if (question.difficulty === 'Medium') { %> bg-warning 
            <% } else { %> bg-danger <% } %>">
            <%= question.difficulty %>
          </span>
        </p>
        <hr>
        <h5>Problem Statement</h5>
        <p><%= question.problem_statement %></p>

        <h6 class="mt-4">Input Format</h6>
        <pre class="bg-light p-2 rounded"><%= question.input_format %></pre>

        <h6 class="mt-4">Output Format</h6>
        <pre class="bg-light p-2 rounded"><%= question.output_format %></pre>

        <h6 class="mt-4">Constraints</h6>
        <pre class="bg-light p-2 rounded"><%= question.constraints %></pre>

        <h6 class="mt-4">Example</h6>
        <p><strong>Input:</strong></p>
        <pre class="bg-light p-2 rounded"><%= question.sample_input %></pre>
        <p><strong>Output:</strong></p>
        <pre class="bg-light p-2 rounded"><%= question.sample_output %></pre>
      </div>

      <!-- Right Half -->
      <div class="col-md-6 position-relative p-4">
        <form action="/coding/<%= question.id %>/submit" method="POST" class="h-100 d-flex flex-column">
          <div class="mb-2">
            <label for="language" class="form-label fw-semibold">Language:</label>
            <select id="language" name="language" class="form-select form-select-sm w-50">
              <option value="cpp">C++</option>
              <option value="java">Java</option>
              <option value="python">Python</option>
            </select>
          </div>

          <textarea name="code" id="code" class="editor-area" placeholder="// Write your code here..."></textarea>

          <div class="d-flex justify-content-end gap-2 mt-3">
            <button type="button" class="btn btn-secondary px-4">Run</button>
            <button type="submit" class="btn btn-success px-4">Submit</button>
          </div>
        </form>
      </div>
    </div>
  </div>

  <footer class="text-center py-3 mt-2 bg-light border-top">
    <p class="mb-0 text-secondary">&copy; 2025 Path2Tech | All Rights Reserved</p>
  </footer>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
